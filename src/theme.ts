import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#157275",
    },
    secondary: {
      main: "#F0F4F4",
    },
  },
});

export default theme;
